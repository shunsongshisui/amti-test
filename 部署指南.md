# 网站国内部署指南

> 目标：让"我的资料"站点在微信 / Safari 里稳定、丝滑地打开（目前 `github.io` 在大陆网络下会被干扰/拦截）。
> 站点结构已确认：**全部资源为相对路径、无外部依赖**，放到任何域名或子目录都能直接运行，无需改代码。

> **状态（2026-08）**：已决定暂继续使用 GitHub。Gitee Pages 已下架（路线 A 存档作废）；若日后要国内稳定访问，走路线 B（备案域名）。

---

## 路线 A：Gitee Pages（已下架 · 仅存档）

> ⚠️ 该服务已因政策原因于 2024 年下架，新用户无法开通，以下步骤不再可用，仅留档备查。

**特点**：免费、用 `gitee.io` 域名、**无需 ICP 备案**，只需实名认证。

### 步骤
1. **注册 Gitee**（gitee.com），完成**实名认证**（要身份证，个人设置里）。
2. 新建一个**公开仓库**，名字随意（比如 `my-site`）。
3. 把本项目内容推上去（保持目录结构，根目录就是站点根）：
   ```bash
   cd "C:/Users/sw/Desktop/我的资料"
   git remote add gitee https://gitee.com/<你的用户名>/my-site.git
   git push gitee main
   ```
4. 仓库页面 → **服务 → Gitee Pages** → 点击**启动**（选部署分支 `main`，目录留空）。
5. 等约 1 分钟，得到网址：`https://<你的用户名>.gitee.io/my-site/`

### 风险提示
- Gitee 免费 Pages 服务对新用户**时灵时不灵**：部分账号申请被拒，或要求先实名满 3 天、或要求付费。
- 若被拒，走路线 B。

---

## 路线 B：最稳 · 一劳永逸 —— 腾讯云 COS + 备案域名（推荐长期）

**特点**：正规、稳定，微信/Safari 全部秒开且不弹拦截。**需要域名 + ICP 备案**（备案要 3~20 天）。

### 步骤
1. **买域名**：腾讯云或阿里云搜"域名注册"，几十块/年，推荐 `.com` 或 `.cn`。
2. **ICP 备案**：腾讯云控制台 → 备案（需身份证 + 人脸核验，通常 3~20 天）。备案期间网站还不能用域名访问。
3. **开通 COS**：控制台 → 对象存储 COS → 创建存储桶（地区选离你近的，如"成都"）→ 开启**静态网站托管**（基础配置里打开"静态网站"）。
4. **上传站点**：把"我的资料"里的所有文件（保持目录结构）上传到桶根目录。
5. **绑定域名 + CDN**：存储桶 → 域名管理 → 绑定你备案好的域名 → 开启 CDN 加速（腾讯云 CDN，勾选"回源到 COS"）。
6. 访问 `https://你的域名.com` 即完成。

---

## 路线 C：过渡 · 不用备案 —— 腾讯云 CloudBase / EdgeOne Pages 免费额度

**特点**：注册腾讯云（实名）即可用**免费额度**开静态托管，先拿到一个临时域名顶着用，之后再补域名备案。

### 步骤
1. 注册腾讯云账号并**实名认证**。
2. 搜索并开通 **EdgeOne Pages** 或 **CloudBase 静态托管**。
3. 把项目文件夹上传 / 关联 Git 仓库，得到一个默认域名。
4. 先用默认域名访问；有正式域名后绑定 + 备案升级为路线 B。

---

## 通用说明

- **结构**：根目录是站点入口（`index.html` 合集页），`psy-age-test/` 是心理年龄测验，其余 `.html` 是各项目页面。
- **路径**：全部使用相对链接（`href="psy-age-test/index.html"` 等），放到任意子路径下都能工作，部署时**保持目录结构**即可。
- **无依赖**：不引任何 CDN / 外部库，离线可跑。
- **每次更新**：Gitee/腾讯云都支持推仓库后手动或自动发布；改完代码 `git push` 到对应远端即可。

---

*由 愿安 撰写 · 2026-08*
